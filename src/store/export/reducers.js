import
{
    // ActionType
} from './actions'

const initialState = {

};

export const exportReducer = (state = initialState, action) =>
{
    switch (action.type) {
        default: return state;
    }
};